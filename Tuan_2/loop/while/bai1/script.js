let num = +prompt("Enter a number: ");
let total = 0;
while( num !== -1 ) {
    myNumber = +prompt("Enter a number: ");
    alert(num);
    alert(myNumber);
    //phần code tính tổng
    total = num + myNumber;
    alert(total);
}